<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h1 class="display-4 mb-4">Welcome to Contact List Web</h1>
                    <h2 class="mb-0">
                        Please 
                        <a asp-controller="Auth" 
                           asp-action="Login" 
                           class="btn btn-primary mx-2">Login</a> 
                        or 
                        <a asp-controller="Auth" 
                           asp-action="Register" 
                           class="btn btn-success mx-2">Register</a> 
                        to manage your contacts.
                    </h2>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Bootstrap CSS (add to your layout file or head section) -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

<!-- Bootstrap JS (add to your layout file before closing body tag) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>